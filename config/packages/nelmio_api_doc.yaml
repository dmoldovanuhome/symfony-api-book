nelmio_api_doc:
    documentation:
        info:
            title: Book API
            description: This is RESTfull API for books
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
